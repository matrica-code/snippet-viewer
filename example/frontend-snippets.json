{
  "react-props@Counter.tsx": "export interface CounterProps {\n  initial?: number;\n  label?: string;\n}",
  "react-hook@Counter.tsx": "function useCounter(initial = 0) {\n  const [count, setCount] = useState(initial);\n  const increment = useCallback(() => setCount((c) => c + 1), []);\n  return { count, increment };\n}",
  "react-component@Counter.tsx": "export function Counter({ initial = 0, label = \"Count\" }: CounterProps) {\n  const { count, increment } = useCounter(initial);\n  return (\n    <button type=\"button\" onClick={increment}>\n      {label}: {count}\n    </button>\n  );\n}",
  "ts-class@FuturesModel.ts": "@kosModel({ modelTypeId: MODEL_TYPE, singleton: true })\n@kosLoggerAware()\n@kosFutureAware()\nexport class FuturesModelImpl\n  implements IKosDataModel, IKosIdentifiable, FutureContainer\n{\n  static Registration: KosModelRegistrationType<FuturesModelImpl>;\n  id: string;\n\n  // A token the rendered docs must never show — proves `ignore` strips it.\n\n  constructor(modelId: string) {\n    this.id = modelId;\n  }\n\n  @kosFuture()\n  async start(trackerId?: string): Promise<unknown | null> {\n    const [err, data] = await startFuture(trackerId || \"\");\n    if (err) {\n      return null;\n    }\n    return data ?? null;\n  }\n\n  get progress(): number | undefined {\n    return this.future?.progress;\n  }\n}",
  "ts-method@FuturesModel.ts": "@kosFuture()\nasync start(trackerId?: string): Promise<unknown | null> {\n  const [err, data] = await startFuture(trackerId || \"\");\n  if (err) {\n    return null;\n  }\n  return data ?? null;\n}",
  "ts-property@FuturesModel.ts": "get progress(): number | undefined {\n  return this.future?.progress;\n}"
}
